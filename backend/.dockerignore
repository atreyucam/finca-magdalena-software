# Dependencias locales (CRÍTICO: evita conflictos de SO y acelera el build)
node_modules

# Logs y debugs
npm-debug.log
yarn-error.log
*.log

# Git y control de versiones
.git
.gitignore

# Archivos de entorno (Seguridad: no quemar secretos en la imagen)
.env
.env.*

# Configuración de Docker (no hace falta dentro del contenedor)
Dockerfile
docker-compose.yml
.dockerignore

# Archivos de IDEs
.vscode
.idea

# Documentación y otros
README.md
LICENSE
coverage
dist
build